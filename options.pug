mixin labeledinput(type, name)
  label(for=name)= name
  input(type=type,name=name,id=name)
mixin labeledarea(name)
  label(for=name)= name
  textarea(name=name,id=name)&attributes(attributes)
doctype html
html
  head
    if !chrome
      link(rel='stylesheet',type='text/css',href='dialog-polyfill.css')
    style.
      #status {
        height: 1.5em;
      }
      th {
      }
      dialog h3 {
        margin: 0;
      }
      th {
        background-image: linear-gradient(rgb(200, 200, 255), rgb(200, 200, 255) 38%, rgb(180, 180, 255));
        box-shadow: rgba(0, 0, 0, 0.08) 0px 1px 0px, rgba(255, 255, 255, 0.75) 0px 1px 2px inset;
        text-shadow: rgb(240, 240, 240) 0px 1px 0px;
      }
      td {
        background-color: rgb(240, 240, 240);
      }
      td.checkbox {
        text-align: center;
      }
      #controlhead {
        display: flex;
      }
      .btn {
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: default;
        font-family: inherit;
        font-size: inherit;
        padding: 0.1em 0.5em;
        margin: 1px;
        border: solid 2px;
        border-radius: 5px;
        border-color: #8080ff;
        box-sizing: content-box;
        color: black;
        text-decoration: none;
        min-height: 2em;
        min-width: 4em;
        background-image: linear-gradient(rgb(237, 237, 237), rgb(237, 237, 237) 38%, rgb(222, 222, 222));
        box-shadow: rgba(0, 0, 0, 0.08) 0px 1px 0px, rgba(255, 255, 255, 0.75) 0px 1px 2px inset;
        text-shadow: rgb(240, 240, 240) 0px 1px 0px;
        border-color: rgba(0, 0, 0, 0.25);
      }
      .btn:hover {
        /*background-color: #ccccff;*/
        background-image: linear-gradient(rgb(240, 240, 240), rgb(240, 240, 240) 38%, rgb(224, 224, 224));
        box-shadow: rgba(0, 0, 0, 0.12) 0px 1px 0px, rgba(255, 255, 255, 0.95) 0px 1px 2px inset;
        border-color: rgba(0, 0, 0, 0.3);
      }
      label[for="query"] {
        vertical-align: top;
      }
      #confirmMessage {
        margin-bottom: 1em;
      }
      #import {
        display: none;
      }
      #dirtyAlert {
        font-weight: bold;
        color: #ff0000;
      }
  body
    div#controlhead
      a.btn#export(download='SearchExtender.conf.json') Export...
      label(for='import'): a.btn Import...
      input.btn#import(type='file')
      input.btn#new(type='button',value='New...')
      input.btn#newFromText(type='button',value='New from text...')
      input.btn#save(type='button',value='Save')
      span#dirtyAlert
    table#table
      tr
        th Edit
        th Name
        th OMNI
        th PAGE
        th SEL
        th LINK
        th IMG
        th Key
        th Query
        th CurTab
        th POST
        th Del
    #status
    dialog#detail
      h3#heading
      hr
      form(method='dialog')
        +labeledinput('text', 'name')
        +labeledinput('checkbox', 'omnibox')
        +labeledinput('checkbox', 'page')
        +labeledinput('checkbox', 'selection')
        +labeledinput('checkbox', 'link')
        +labeledinput('checkbox', 'image')
        br
        +labeledinput('text', 'key')
        br
        +labeledarea('query')(rows='5',cols='80')
        br
        +labeledinput('checkbox', 'curtab')
        +labeledinput('checkbox', 'post')
        br
        input#update(type='submit',value='')
        input#cancel(type='reset',value='Cancel')
    dialog#fromText
      h3 New from text
      hr
      form(method='dialog')
        textarea#entry(rows='10',cols='80')
        br
        input#confirmFromText(type='submit',value='Add')
        input#cancelFromText(type='reset',value='Cancel')
    dialog#confirm
      h3 Confirmation
      hr
      #confirmMessage
      form(method='dialog')
        input#confirmConfirm(type='submit',value='Confirm')
        input#cancelConfirm(type='reset',value='Cancel')
    dialog#alert
      h3#alertTitle
      input#alertConfirm(type='submit',value='OK')
    if !chrome
      script(src='dialog-polyfill.js')
    script(src='multiua.js')
    script(src='options.js')
